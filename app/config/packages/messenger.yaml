framework:
    messenger:
        transports:
            async:
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
                options:
                    auto_setup: false

        routing:
            'App\Module\LeaveRequest\Message\LeaveRequestAutoApprovedMessage': async
            'App\Infrastructure\Email\Message\SendLeaveRequestEmailMessage': async
            'App\Infrastructure\Email\Message\SendInvitationEmailMessage': async

when@test:
    framework:
        messenger:
            transports:
                async: 'in-memory://'
