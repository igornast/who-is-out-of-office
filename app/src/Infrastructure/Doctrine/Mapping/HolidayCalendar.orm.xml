<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                          https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="App\Infrastructure\Doctrine\Entity\HolidayCalendar" table="holiday_calendar">
        <id name="id" column="id" type="integer" length="10">
            <generator strategy="AUTO"/>
        </id>

        <field name="countryCode" column="country_code" length="4"/>
        <field name="countryName" column="country_name" length="100"/>

        <one-to-many
                field="holidays"
                target-entity="App\Infrastructure\Doctrine\Entity\Holiday"
                mapped-by="holidayCalendar"
                orphan-removal="true">
            <cascade>
                <cascade-persist/>
                <cascade-remove/>
            </cascade>
        </one-to-many>

        <field name="createdAt" column="created_at" type="datetime_immutable"/>
        <field name="updatedAt" column="updated_at" type="datetime_immutable"/>

        <lifecycle-callbacks>
            <lifecycle-callback type="preUpdate" method="updateTimestamp" />
        </lifecycle-callbacks>
    </entity>
</doctrine-mapping>